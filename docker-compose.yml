version: '3.3'

services:
  mongo:
    image: mongo:4.4
    environment:
      MONGO_INITDB_ROOT_USERNAME: superroot
      MONGO_INITDB_ROOT_PASSWORD: superroot123
    ports:
      - 27017:27017
